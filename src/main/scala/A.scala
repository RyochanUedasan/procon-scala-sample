import scala.io.StdIn._

// https://atcoder.jp/contests/abs/tasks/abc086_a
// 提出する時はobject名をMainにする
object A extends App {
  val s: String = readLine
  val seq: Seq[String] = s.split(" ")
  val nums: Seq[Int] = seq.map(_.toInt)
  val a = nums(0)
  val b = nums(1)
  // ここに処理を書く
  println()
}